export type Id = string | number;

export type Column = {
  id: Id;
  title: string;
};

export type Position = {
  xRate: number;
  yRate: number;
};
export type Size = {
  height: number;
  width: number;
};